<p style="color: green"><%= notice %></p>

<%= render @category %>
<% @entities.each do |entity| %>
  <%= render "entity", entity: entity %>
<% end %>

<div>
  <%= button_to "<", categories_path, method: :get %>

  <%= button_to "Add a new transaction", new_category_entity_path([params[:id]]), method: :get %>
</div>
